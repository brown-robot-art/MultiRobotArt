import { WorkspaceSvg } from "blockly";
import { UseBlocklyProps } from "./BlocklyWorkspaceProps";
declare const useBlocklyWorkspace: ({ ref, initialXml, toolboxConfiguration, workspaceConfiguration, onWorkspaceChange, onImportXmlError, onInject, onDispose, }: UseBlocklyProps) => {
    workspace: WorkspaceSvg | null;
    xml: string | null;
};
export default useBlocklyWorkspace;
